import React from 'react';
import styles from "./SendMessagePanel.module.css" 

const SendMessagePanel = (props) => {
    return (
        <div className="SendMessagePanel">
            
        </div>
    );
};

export default SendMessagePanel;