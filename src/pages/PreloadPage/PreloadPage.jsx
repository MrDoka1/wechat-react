import React from 'react';
import styles from "./PreloadPage.module.css"

const PreloadPage = (props) => {
    return (
        <div className={styles.wrapper}>
            <div className={styles.text}>Connect to server...</div>
        </div>
    );
};

export default PreloadPage;