import React from 'react';
import styles from "./WindowSearch.module.css"

const WindowSearch = (props) => {
    return (
        <div className={styles.wrapper}>

        </div>
    );
};

export default WindowSearch;